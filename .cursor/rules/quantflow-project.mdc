---
description: QuantFlow project front door and documentation routing
alwaysApply: true
---

# QuantFlow routing

**Branch:** `quantflow-v4` (extends v3; v3 docs remain binding authority)

**Read order (on `quantflow-v4`):**

1. `AGENTS.md` - agent guardrails and active-branch routing
2. `docs/v4/V4_TERRITORY_MAP.md` - v4 territory + open decisions (locked reference)
3. `BUILD_PLAN_V4.md` - current v4 rung goal shape (R0–R7) + cumulative regression guard
4. `docs/v4/AGENTS.md` - v4 subtree contract
5. `KERNEL_CONSTITUTION.md` + `docs/v3/AUTHORITY_RULES.md` + `docs/v3/KERNEL_SCHEMA_V1.md` + `docs/v3/GLOSSARY.md` - still binding
6. `BUILD_PLAN_V3.md` - shipped v3 ladder + goal-shape convention (reference)
7. `CONCEPT.md` / `VAULT.md` - product definition / paired vault paths

On `quantflow-v3`, use `BUILD_PLAN_V3.md` as the execution plan instead (steps 3–4 above).

**Vault:** `C:\Users\rybow\Obsidian\Cursor Collab`

Fresh root references:

- `V3 Official.md`
- `Cursor Roadmap.md`
- `QuantFlow_Integration_Map.md`
- `Fable URL List.md`
- `QuantFlow Constructor Theory.md`

Older but still relevant context:

- `Projects/QuantFlow/reference/`
- repo `reference/`

Do not execute from archived plans, old layer charters, or old vault build plans unless `BUILD_PLAN_V3.md` explicitly points to them.

**v3 spine rule:** Kernel owns truth. Canvas renders truth. Conductor plans. Workers execute. Receipts prove. State Cards summarize. Harnesses adapt runtimes.

**Keep:** infinite canvas, herdr-backed terminal tiles, Envoy task bus, Obsidian mirror, MCP as external adapter.

**Do not drift into:** Cloudflare-first rewrite, RL training, A2A/Agent Cards, custom string relay revival, dashboard replacement, or bypassing Kernel commands.

**Handoff:** commit before switching agents. Include branch, active v4 rung (R0–R7 from `BUILD_PLAN_V4.md`), files changed, proof commands (the rung's cumulative regression stack), reference notes used, and risks.
