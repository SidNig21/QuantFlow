---
description: QuantFlow project front door and documentation routing
alwaysApply: true
---

# QuantFlow project routing

**Active branch:** `quantflow-v2`

**Authority docs (repo root):** `ARCHITECTURE.md` + `BUILD_PLAN_V2.md`

Execute only from `BUILD_PLAN_V2.md`. Read `ARCHITECTURE.md` for layer boundaries.

Do not implement from `QuantFlow` branch assumptions, `Projects/QuantFlow/reference/`, or `reference/archive/`.

**v1 (frozen on branch `QuantFlow`):** vault `Projects/QuantFlow/Build Plan.md` — not execution authority on this branch.

**Supplementary guardrails (still in force):** herdr = WSL session authority; node-pty = PTY bridge display (herdr attach in WSL) + Windows shell fallback; direct socket→xterm blocked (0a); Envoy = one canvas space; dumb tiles never call Envoy directly.

Vault context (optional): `Obsidian/Cursor Collab/Projects/QuantFlow/How the pieces fit.md`
