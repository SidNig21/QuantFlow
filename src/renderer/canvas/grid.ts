export const CANVAS_GRID_TOKENS = Object.freeze({
  columns: 12,
  columnWidth: 80,
  gutter: 24,
  margin: 32,
  baseline: 8,
  majorBaseline: 64,
  stringLane: 24,
});

export type CanvasGridTokens = typeof CANVAS_GRID_TOKENS;
