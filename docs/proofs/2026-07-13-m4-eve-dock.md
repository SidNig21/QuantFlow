# M4 Eve Dock Surface

**Status:** passed locally on 2026-07-13

The native Mac app now has one promoted dock species: **Eve**. Clicking it
performs this ordered, governed operation:

```text
Swift dock click
  → Kernel tile.create + worker.create
  → native AgentOS Eve session attach
  → Kernel worker.runtime.bind
  → Swift Eve session sheet
```

The Kernel persists the actor and AgentOS session identifiers on the worker;
the session sheet keeps only display transcript entries. It cannot create a
second workflow-state store.

The dock remains disabled unless the sidecar explicitly reports `promptable`.
This prevents an absent model credential from producing a durable-but-stranded
tile or a fake-ready input field.

## Verified without credentials

- `swift build` and `swift run QuantFlowKernelProof` pass. The proof verifies
  runtime binding survives a Kernel snapshot.
- `QUANTFLOW_RUNTIME_ROOT=tools/agentos-host-mac swift run QuantFlowRuntimeProof`
  proves that the native Swift supervisor launches the local sidecar and sees
  Eve plus AgentOS ready without overstating credentialless promptability.
- Native sidecar `POST /v1/agentos/eve-session` creates a real Eve ACP session
  for `["m4-proof", "eve-tile-a"]` and returns HTTP `201`.
- A prompt without `OPENCODE_GO_API_KEY` returns HTTP `409` with an explicit
  explanation. The Swift UI displays that error; it never calls this state
  promptable.

## Live acceptance result

With the credential inherited only by the sidecar process, the isolated
`npm run proof:m4-live` acceptance passed:

- promptable health: true
- warm AgentOS Eve attach: **834.5 ms**
- Eve reply to the deterministic prompt: **`PONG`**

## Repeat the live proof

Set `OPENCODE_GO_API_KEY` in the environment, then run the isolated proof:

```bash
cd /Users/r/Documents/QuantFlow\ Mac/tools/agentos-host-mac
OPENCODE_GO_API_KEY="…" npm run proof:m4-live
```

It asserts all live runtime conditions behind one native dock click: promptable
health, a warm AgentOS Eve attach at or below one second, and an actual `PONG`
reply. The Swift dock uses this same `RuntimeClient` attachment/prompt path;
the app keeps it disabled unless that health state is promptable.
