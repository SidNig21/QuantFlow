# QuantFlow Agent Entry

Use two files only:

1. `CONCEPT.md`
2. `BUILD_PLAN_V2.md`

`BUILD_PLAN_V2.md` is the only execution plan. Do not execute from archived layer charters or Obsidian v1 build plans.

Current branch: `quantflow-v2`

Before coding:

```bash
git pull origin quantflow-v2
git status --short --branch
```

If the requested work is not the current slice in `BUILD_PLAN_V2.md`, ask the operator before coding.
